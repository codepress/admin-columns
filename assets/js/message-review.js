'use strict';jQuery(function($){$(document).ready(function(){$('.updated a.hide-review-notice').click(function(e){e.preventDefault();var el=$(this).parents('.ac-message');var el_close=el.find('.hide-notice');var soft=$(this).hasClass('hide-review-notice-soft');if(soft){el.find('.info').slideUp();el.find('.help').slideDown()}else{el_close.hide();el_close.after('<div class="spinner right"></div>');el.find('.spinner').show()}$.post(ajaxurl,{'action':'ac_hide_notice_review'},function(){if(!soft){el.find('.spinner').remove();el.slideUp()}});return false})})});