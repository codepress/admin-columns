'use strict';jQuery(function($){$(document).on('click','.ac-notice [data-dismiss]',function(e){e.preventDefault();var $notice=$(this).parents('.ac-notice');var action=$notice.data('action');var nonce=$notice.data('nonce');$notice.fadeOut(500,function(){$notice.remove()});$.post(ajaxurl,{action:'ac_notice_dismiss_'+action,name:action,_ajax_nonce:nonce})})});